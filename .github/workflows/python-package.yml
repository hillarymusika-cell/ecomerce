# Deprecated – replaced by ci.yml
# Kept as a no-op redirect so old badge URLs do not fail loudly.
name: Python package

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use the CI workflow instead."
